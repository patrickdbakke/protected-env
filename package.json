{
  "name": "protect-env",
  "version": "1.0.0",
  "description": "A naive way to protect your process.env",
  "main": "protect-env.js",
  "scripts": {
    "test": "NODE_ENV=spec istanbul cover _mocha -- ./**/*.spec.js --require ./spec/mocha.opts.js",
    "prettier": "find ./ -name '*.js' -not -path './node_modules/*' | xargs ./node_modules/.bin/prettier --write --single-quote --trailing-comma es5"
  },
  "author": "patrickdbakke",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.1.1",
    "istanbul": "^0.4.5",
    "mocha": "^3.5.0",
    "prettier": "^1.5.3"
  },
  "dependencies": {
    "minimatch": "^3.0.4"
  },
  "homepage": "https://github.com/patrickdbakke/protected-env",
  "engines": {
    "node": ">=4"
  },
  "keywords": ["process", "env", "environment", "protect"],
  "bugs": "https://github.com/patrickdbakke/protected-env/issues",
  "contributors": ["patrickdbakke", "katelovescode"],
  "repository": {
    "type": "git",
    "url": "https://github.com/patrickdbakke/protected-env.git"
  }
}
